package data.map;

import data.geometry.Position;

public class Extension extends Road {

	public Extension(Position extensionPosition, int extensionAxis, Line line) {
		super(extensionPosition, extensionAxis, line);
	}

}
